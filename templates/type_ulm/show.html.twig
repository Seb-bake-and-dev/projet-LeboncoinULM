{% extends 'base.html.twig' %}

{% block title %}TypeUlm{% endblock %}

{% block body %}
    <h1>TypeUlm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_ulm.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_ulm.Type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_ulm_index') }}">back to list</a>

    <a href="{{ path('type_ulm_edit', {'id': type_ulm.id}) }}">edit</a>

    {{ include('type_ulm/_delete_form.html.twig') }}
{% endblock %}