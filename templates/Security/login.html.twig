{% extends "base.html.twig" %}
{% block content %}
    <div class="container pt-5">
    {% if error %}
        <div class="alert alert-danger">
            {{ error.messageKey }}
        </div>
    {% endif %}
    <div class="row mx-auto my-5 justify-content-center">
        <div class="col-10">
            <div class="well bg-light p-5">
                <div class="text-center text-dark font-weight-bold h3 pb-3">
                    Connexion
                </div>
                <form action="{{ path('security_login') }}" method="post">
                    <fieldset>
                        <div class="input-group mb-2">
                            <div class="input-group-prepend">
                                <div class="input-group-text"><i class="fas fa-user-circle"></i></div>
                            </div>
                            <input class="form-control" type="text" id="username" name="_username"
                                   value="{{ last_username }}" required="required" placeholder="Identifiant"/>
                        </div>
                        <div class="input-group mb-2">
                            <div class="input-group-prepend">
                                <div class="input-group-text"><i class="fas fa-lock"></i></div>
                            </div>
                            <input type="password" id="password" name="_password" class="form-control"
                                   placeholder="Mot de passe"/>
                        </div>
                        <div class="row justify-content-center">
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}"/>
                            <button type="submit" class="btn btn-primary active mt-3 p-2 pl-5 pr-5">
                                Connexion
                            </button>
                        </div>
                    </fieldset>
                </form>
            </div>
        </div>
    </div>
{% endblock %}